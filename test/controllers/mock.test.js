'use strict'

const spt = require('../support')

describe('test/controllers/mock.test.js', () => {
  afterAll(() => spt.cleanCollections())

  describe('getAPI', () => {
    it('test', done => {
      done()
    })
  })
})
